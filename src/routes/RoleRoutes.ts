/*import express from 'express';
import { nuevoRol, obtenerRoles, obtenerRol, actualizarRol, eliminarRol } from '../controllers/roleController'

const router = express.Router();

/** ROUTE - CREAR NUEVO ROL 
router.post('/roles', nuevoRol);  

/** ROUTE - OBTENER ROLES 
router.get('/roles', obtenerRoles);  

/** ROUTE - OBTENER ROL POR ID 
router.get('/roles/:id', obtenerRol);

/** ROUTE - ACTULIZAR UN ROL 
router.put('/roles/:id', actualizarRol);   
/** ROUTE - ELIMIAR ROL 
router.delete('/roles/:id', eliminarRol);   

export default router;
*/